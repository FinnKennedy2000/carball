{
  "name": "carsoccer",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "trap 'kill 0' EXIT; vite build --watch & node --env-file-if-exists=.env --watch server/index.js & wait",
    "build": "vite build",
    "start": "node --env-file-if-exists=.env server/index.js",
    "test": "node --test 'test/*.test.js'",
    "db:push": "supabase db push --password \"$SUPABASE_DB_PASSWORD\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "three": "^0.171.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "vite": "^6.0.7"
  }
}
