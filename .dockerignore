# node_modules must not come from the host: the image installs its own, and
# native binaries built elsewhere would overwrite them.
node_modules
dist

# Never bake secrets into an image layer. The service key lives here.
.env
.env.*
!.env.example

.git
.github
.serena
.remember
test
*.md
